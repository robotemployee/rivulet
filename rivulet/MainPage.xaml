<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:rivulet="clr-namespace:rivulet"
             xmlns:drawables="clr-namespace:rivulet.Graphics.Drawables"
             x:Class="rivulet.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            <Label
                x:Name="CurrentTimeLabel"
                HorizontalOptions="Center"
                FontSize="Header"></Label>
            
            <Grid HeightRequest="300" WidthRequest="300" HorizontalOptions="Center">
                <GraphicsView x:Name="CircleGraphicsView">
                    <GraphicsView.Drawable>
                        <drawables:CircleDrawable />
                    </GraphicsView.Drawable>
                </GraphicsView>
                <Label Text="Circle"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       TextColor="Red" 
                       FontSize="24"/>
            </Grid>
            
            
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
